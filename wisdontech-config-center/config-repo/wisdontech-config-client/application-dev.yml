info:
  profile: dev
  from: git
server:
  port: 8060
spring:
  cloud:
    config:
      server:
        git:
          uri: http://git.wisdontech.com/wisdontech/project/config/wisdontech-service-config.git
          searchPaths: config-repo, config-repo/wisdontech-config-server
          username: deploy
          password: deploy.1234  
logging:
  path: /opt/spring-boot-project/log/${spring.application.name}-${server.port}
  level:
    root: INFO
    org:
      springframework: INFO