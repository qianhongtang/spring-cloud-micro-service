server:
  port: 8060
spring:
  application:
    name: config-client
  cloud:
    consul:
      host: localhost
      port: 8500
    config:
      profile: default
      label: master
      discovery:
        enabled: true
        serviceId: config-server